/* import React, {useEffect} from 'react';
import {useParams} from 'react-router-dom'
import blogData from '../json/clanci'



const CardItem=({})=>{

    const {postID}=useParams();
    const {text}=blogData.data

    return(
        <h1>
            {text}
        </h1>
    )
}

export default CardItem */